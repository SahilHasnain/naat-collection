import ProcessedAudioClient from "@/components/admin/ProcessedAudioClient";

export const metadata = {
  title: "Processed Audio Review - Admin",
  description: "Review and manage AI-processed audio files",
};

export default function ProcessedAudioPage() {
  return <ProcessedAudioClient />;
}
